@page "/listar-etiquetas"
@using GestorTareas.Client.Components.Etiqueta

<MudText Align="Align.Center" Typo="Typo.h4" Color="Color.Primary">
    Listar Etiqueta
</MudText>
@if (Etiquetas is null)
{
    <MudText Typo="Typo.body1">
        Cargando...
    </MudText>
}
else
{
    @if (Etiquetas.Any())
    {

        <div class="m-6">
            @*Muestra un listado con todas las etiquetas*@
            @foreach (var etiqueta in Etiquetas)
            {
                <MudChip Color="Color.Surface" Size="Size.Large">@etiqueta.Name</MudChip>
            }
        </div>

    }
    else
    {
        <MudText Typo="Typo.body1">
            No se han encontrado etiquetas
        </MudText>
    }
}