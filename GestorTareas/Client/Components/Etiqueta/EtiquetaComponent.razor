@*<MudCard Elevation="3">
    <MudCardContent>
        <MudText>@Etiqueta.Name</MudText>
    </MudCardContent>
    @if (Delete || Modify)
    {
        <MudCardActions>
            @if (Modify)
            {
                <MudButton Variant="Variant.Text" Color="Color.Warning" OnClick="@ModifyEtiquetaAsync">Editar</MudButton>
            }

            @if (Delete)
            {
                <MudButton Variant="Variant.Filled" Color="Color.Error" OnClick="@DeleteEtiquetaAsync">Eliminar</MudButton>
            }
        </MudCardActions>
    }

</MudCard>*@

@if (Modify)
{
    <MudChip Color="Color.Warning" Style="color: black" OnClose="ModifyEtiquetaAsync" Size="Size.Large" CloseIcon="@Icons.Filled.Edit">@Etiqueta.Name</MudChip>
}

@if(Delete)
{
    <MudChip Color="Color.Error" OnClose="DeleteEtiquetaAsync" Size="Size.Large">@Etiqueta.Name</MudChip>
}